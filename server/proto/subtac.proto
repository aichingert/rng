syntax = "proto3";

package suptac;

message JoinRequest {
  string user = 1;
}

message Message {
  string content = 1;
}

message Empty {}

service Lobby {
  rpc JoinLobby (JoinRequest) returns (stream Message);
  rpc SendMessage (Message) returns (Empty);
}

