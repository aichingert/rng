syntax = "proto3";

import "proto/auth.proto";

message Token {
    string access_token = 1;
}

message JoinRequest {
    int32 id = 1;
    Token token = 2;
}

service Channel {
    rpc JoinChannel (JoinRequest) returns (Empty);
}
